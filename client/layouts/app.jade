template(name='app')
    nav.navbar.navbar-default.navbar-fixed-top
        .container
            .navbar-header
                button.navbar-toggle.collapsed(
                type='button',
                data-toggle='collapse',
                data-target='#navbar',
                aria-expanded='false',
                aria-controls='navbar'
                )
                    span.sr-only {{ i18n 'general.toggle_nav' }}
                    span.icon-bar
                    span.icon-bar
                    span.icon-bar
                a.navbar-brand(href='/') {{ i18n 'general.logo' }}
            #navbar.collapse.navbar-collapse
                ul.nav.navbar-nav
                    li
                        a(href='/') {{ i18n 'homepage.name' }}
                    li
                        a(href='/deps') {{ i18n 'deps.name' }}
                    li
                        a(href='/editor') Boards
                ul.nav.navbar-nav.navbar-right
                    +loginButtons
    .container
        +yield
    .footer
        .container
            p.text-muted TODO List, 2014.
